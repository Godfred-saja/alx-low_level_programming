#include "main.h"
/**
* _abs - function that computes the absolute value of an integer
*
* @n: the int to check code
* Return: the absolute values of int
*/
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
